<?xml version="1.0" encoding="UTF-8" ?>
    <!DOCTYPE mapper PUBLIC   
        "-//mybatis.org//DTD Mapper 3.0//EN"  
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--这块等于dao接口的实现 namespace必须和接口的类路径一样 -->
<mapper namespace="com.yummy.dao.PersonMapper">
	<!-- findById必须和接口中的方法名一样 返回一个person 就是刚才的别名 如果不弄别名要连类路径一起写 麻烦 -->
	<select id="findById" parameterType="HashMap" resultType="com.yummy.model.Person">
		select
		* from person where id=#{id}
	</select>
	<!-- getAllPerson接口中的方法名一样 返回list person-->
	<select id="getAllPerson" parameterType="string" resultMap="resultListPerson">
		select
		* from person
	</select>
	<!-- 为了返回list 类型而定义的returnMap -->
    <resultMap type="com.yummy.model.Person" id="resultListPerson">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="age" property="age" />
        <result column="sex" property="sex" />
    </resultMap>
</mapper> 
